{
  "name": "cspicks",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "postbuild": "rm -f dist/professor_history_openalex.json dist/school-aliases.json",
    "preview": "vite preview",
    "predeploy": "npm run build && npm run postbuild",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^7.2.4"
  },
  "dependencies": {
    "chart.js": "^4.5.1",
    "he": "^1.2.0",
    "papaparse": "^5.5.3"
  }
}