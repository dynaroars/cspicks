{
  "name": "cspicks",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "vite",
    "test": "node --test test/unit/*.test.js",
    "test:e2e": "playwright test",
    "check:size": "node scripts/check-project-size.mjs",
    "sync:csrankings-rules": "node scripts/sync-csrankings-rules.mjs",
    "sync:nsf": "node scripts/sync-nsf-awards.mjs",
    "sync:nsf:all": "node scripts/sync-nsf-awards.mjs --all-us",
    "sync:nsf:names": "node scripts/sync-nsf-roster-names.mjs",
    "compact:history": "node scripts/compact-openalex-history.mjs",
    "sync:nsf:rebuild": "node scripts/sync-nsf-awards.mjs --all-us --cache-only",
    "og:image": "node scripts/generate-og-image.mjs",
    "sitemap": "node scripts/generate-sitemap.mjs",
    "build": "vite build",
    "postbuild": "rm -f dist/professor_history_openalex.json dist/school-aliases.json && rm -rf dist/data",
    "preview": "vite preview",
    "predeploy": "npm run build && npm run postbuild",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "gh-pages": "^6.3.0",
    "vite": "^7.2.4"
  },
  "dependencies": {
    "chart.js": "^4.5.1",
    "he": "^1.2.0",
    "papaparse": "^5.5.3"
  }
}
